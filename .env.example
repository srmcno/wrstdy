# Optional. If set, baked into the build as the default Anthropic API key for
# Step 7 (AI Analysis). Leave commented out to make staff paste their own key
# via the in-app Settings panel.
#
# VITE_ANTHROPIC_KEY=sk-ant-...
